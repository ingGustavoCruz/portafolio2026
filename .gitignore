# ================================================================
# .gitignore — Portfolio Ing. Gustavo Cruz
# ================================================================

# ── Archivos sensibles — NUNCA subir a GitHub ───────────────────
api/db.php
.env
.env.local
.env.production

# ── Dependencias de Composer ─────────────────────────────────────
/vendor/
composer.lock

# ── Archivos del sistema operativo ──────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editores e IDEs ──────────────────────────────────────────────
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.code-workspace

# ── Logs y temporales ────────────────────────────────────────────
*.log
*.tmp
*.swp
*.swo
*~

# ── Archivos de caché ────────────────────────────────────────────
.cache/
*.cache

# ── Archivos de backup ───────────────────────────────────────────
*.bak
*.backup
*.sql.gz

# ── Uploads de usuarios (si los agregas después) ─────────────────
assets/uploads/

# ── Node (por si agregas herramientas de build después) ──────────
node_modules/
npm-debug.log*
package-lock.json